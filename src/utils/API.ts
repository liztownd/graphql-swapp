import axios from 'axios';

const API = {
    // getChars(query) {
    //     axios.get('https://swapi.graph.cool', {
    //         method: 'GET',
    //         headers: { 'Content-Type': 'application/json' },
    //         body: JSON.stringify({
    //             query: { query }
    //         }),
    //     }).then(res => res.json())
    //         .then(res => console.log(res.data.allFilms));
    // }
}

export default API